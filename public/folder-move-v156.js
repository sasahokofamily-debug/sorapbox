import { getApps, getApp, initializeApp } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js";
import { getAuth, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";
import { getFirestore, collection, doc, updateDoc, onSnapshot, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";
import { firebaseConfig } from "./firebase-config.js";

const app=getApps().length?getApp():initializeApp(firebaseConfig);
const auth=getAuth(app),db=getFirestore(app),ROOT="root";
let user=null,items=[],unsub=null,timer=0,moveItem=null;

const style=document.createElement("style");
style.textContent=`
#soraMoveDialog{width:min(92vw,540px);max-height:84vh;border:0;border-radius:24px;padding:0;background:#fff;color:#172033;box-shadow:0 28px 90px rgba(15,23,42,.30)}
#soraMoveDialog::backdrop{background:rgba(15,23,42,.50);backdrop-filter:blur(5px)}
.sora-move-head{display:flex;align-items:center;justify-content:space-between;padding:18px 20px;border-bottom:1px solid #e6ebf2}.sora-move-head h2{margin:0;font-size:19px}.sora-move-close{width:36px;height:36px;border:0;border-radius:50%;font-size:20px;background:#f1f4f8}.sora-move-body{padding:16px 18px 20px}.sora-move-name{margin:0 0 13px;font-size:12px;color:#64748b;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.sora-folder-list{display:grid;gap:7px;max-height:54vh;overflow:auto}.sora-folder-choice{display:flex;align-items:center;gap:10px;width:100%;padding:12px;border:1px solid #e1e7ef;border-radius:12px;text-align:left;background:#fff;color:#263449;font:inherit;font-size:13px;font-weight:750}.sora-folder-choice:hover{background:#f4f8ff;border-color:#b9d5ff}.sora-folder-choice.return{background:#eef6ff;border-color:#c9ddfb;color:#165bb5}.sora-folder-choice small{margin-left:auto;color:#8490a3;font-size:10px}.sora-move-status{min-height:19px;margin:11px 0 0;font-size:12px;font-weight:700;color:#64748b}.sora-move-status.err{color:#c0392b}
`;
document.head.appendChild(style);

function ms(v){return !v?0:typeof v.toMillis==="function"?v.toMillis():v.seconds?v.seconds*1000:new Date(v).getTime()||0}
function folderMap(){return new Map(items.filter(x=>x.type==="folder").map(x=>[x.id,x]))}
function currentFolder(){const view=document.querySelector('.nav-item.active')?.dataset.view;if(view&&view!=="files")return ROOT;let parent=ROOT;for(const name of [...document.querySelectorAll("#breadcrumbs button:not(.sora-folder-back)")].slice(1).map(x=>x.textContent.trim())){const f=items.find(x=>x.type==="folder"&&!x.trashed&&(x.parentId||ROOT)===parent&&x.name===name);if(!f)break;parent=f.id}return parent}
function visibleItems(){const view=document.querySelector('.nav-item.active')?.dataset.view||"files",folder=currentFolder(),q=(document.getElementById("searchInput")?.value||"").trim().toLowerCase(),map=new Map(items.map(x=>[x.id,x]));let a=view==="trash"?items.filter(x=>x.trashed&&!map.get(x.parentId)?.trashed):view==="recent"?items.filter(x=>!x.trashed&&x.type==="file"):items.filter(x=>!x.trashed&&(x.parentId||ROOT)===folder);if(q)a=items.filter(x=>(view==="trash"?x.trashed:!x.trashed)&&String(x.name||"").toLowerCase().includes(q));const nameSort=(document.getElementById("sortButton")?.textContent||"").includes("名前");return a.sort((a,b)=>nameSort?String(a.name||"").localeCompare(String(b.name||""),"ja",{numeric:true}):a.type!==b.type&&view==="files"?(a.type==="folder"?-1:1):ms(b.updatedAt||b.createdAt)-ms(a.updatedAt||a.createdAt))}
function folderPath(folder){const map=folderMap(),parts=[];let cur=folder,guard=0;while(cur&&guard++<30){parts.unshift(cur.name||"フォルダ");cur=map.get(cur.parentId)}return parts.join(" / ")}
function dialog(){let d=document.getElementById("soraMoveDialog");if(d)return d;d=document.createElement("dialog");d.id="soraMoveDialog";d.innerHTML=`<div class="sora-move-head"><h2>📁 移動先を選ぶ</h2><button id="soraMoveClose" class="sora-move-close" type="button">×</button></div><div class="sora-move-body"><p id="soraMoveName" class="sora-move-name"></p><div id="soraFolderList" class="sora-folder-list"></div><p id="soraMoveStatus" class="sora-move-status"></p></div>`;document.body.appendChild(d);d.querySelector("#soraMoveClose").onclick=()=>d.close();return d}
function addChoice(list,label,small,id,klass=""){if((moveItem.parentId||ROOT)===id)return;const b=document.createElement("button");b.type="button";b.className=`sora-folder-choice ${klass}`.trim();b.innerHTML=`<span>${label}</span><small>${small}</small>`;b.onclick=()=>moveTo(id,dialog());list.appendChild(b)}
function openMove(item){moveItem=item;const d=dialog(),list=d.querySelector("#soraFolderList"),status=d.querySelector("#soraMoveStatus"),map=folderMap(),current=item.parentId||ROOT;d.querySelector("#soraMoveName").textContent=`「${item.name}」の移動先`;status.textContent="";status.className="sora-move-status";list.replaceChildren();
  if(current!==ROOT)addChoice(list,"⬅ マイファイルへ戻す","一番上",ROOT,"return");
  const currentFolderObj=map.get(current);const upper=currentFolderObj?.parentId||ROOT;if(current!==ROOT&&upper!==current)addChoice(list,"↖ 1つ上のフォルダへ戻す",upper===ROOT?"マイファイル":folderPath(map.get(upper)),upper,"return");
  if(item.previousParentId&&item.previousParentId!==current&&item.previousParentId!==upper)addChoice(list,"↩ 前の場所へ戻す",item.previousParentId===ROOT?"マイファイル":folderPath(map.get(item.previousParentId)),item.previousParentId,"return");
  items.filter(x=>x.type==="folder"&&!x.trashed&&x.id!==item.id&&x.id!==current&&x.id!==upper&&x.id!==item.previousParentId).sort((a,b)=>folderPath(a).localeCompare(folderPath(b),"ja",{numeric:true})).forEach(f=>addChoice(list,`📁 ${f.name}`,folderPath(f),f.id));
  if(!list.children.length){const p=document.createElement("p");p.textContent="移動できる別の場所がありません。";p.style.cssText="font-size:13px;color:#64748b;padding:12px";list.appendChild(p)}if(!d.open)d.showModal()}
async function moveTo(folderId,d){if(!user||!moveItem)return;const status=d.querySelector("#soraMoveStatus"),oldParent=moveItem.parentId||ROOT;status.textContent="移動しています…";d.querySelectorAll(".sora-folder-choice").forEach(b=>b.disabled=true);try{await updateDoc(doc(db,"users",user.uid,"items",moveItem.id),{parentId:folderId,previousParentId:oldParent,updatedAt:serverTimestamp()});status.textContent=folderId===ROOT?"マイファイルへ戻しました。":"移動しました。";setTimeout(()=>d.close(),450)}catch(e){console.error("move file",e);status.textContent="移動できませんでした。";status.className="sora-move-status err";d.querySelectorAll(".sora-folder-choice").forEach(b=>b.disabled=false)}}
function install(){if(!user)return;const visible=visibleItems(),rows=[...document.querySelectorAll("#fileList tr")];rows.forEach((row,i)=>{const item=visible[i],menu=row.querySelector(".row-menu");if(!item||!menu||item.trashed||item.type!=="file")return;row.dataset.soraMoveItemId=item.id;let b=menu.querySelector(".sora-move-file");if(!b){b=document.createElement("button");b.type="button";b.className="sora-move-file";b.textContent="フォルダへ移動 / 戻す";const danger=menu.querySelector(".danger");menu.insertBefore(b,danger||null)}b.onclick=ev=>{ev.preventDefault();ev.stopPropagation();menu.classList.add("hidden");const latest=items.find(x=>x.id===row.dataset.soraMoveItemId);if(latest)openMove(latest)}})}
function schedule(){clearTimeout(timer);timer=setTimeout(install,120)}
onAuthStateChanged(auth,u=>{user=u;items=[];if(unsub){unsub();unsub=null}if(!u)return;unsub=onSnapshot(collection(db,"users",u.uid,"items"),s=>{items=s.docs.map(d=>({id:d.id,...d.data()}));schedule()},e=>console.warn("folder move snapshot",e));setTimeout(schedule,400)});
new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener("click",e=>{if(e.target.closest(".nav-item,#sortButton,#breadcrumbs"))schedule()});document.getElementById("searchInput")?.addEventListener("input",schedule);
console.info("sorapbox folder move v1.5.6 loaded");
